# Placeholder backend Dockerfile (no app code yet)
FROM golang:1.21-alpine AS base
WORKDIR /app
# In later phases, copy go.mod and go.sum, download deps, then build.
CMD ["sh", "-c", "echo Backend image placeholder; sleep 1"]
