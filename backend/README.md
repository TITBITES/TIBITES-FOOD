# Backend (Go) — Clean Architecture Skeleton

Do not add business logic in Phase 1.

Structure:
- cmd/
- internal/
  - handlers/
  - services/
  - domain/
  - ports/
  - adapters/
  - config/

Each folder contains a placeholder .keep file to maintain structure.
